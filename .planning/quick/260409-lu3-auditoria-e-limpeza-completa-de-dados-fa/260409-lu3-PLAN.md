---
quick_task: 260409-lu3
description: "Auditoria e limpeza completa de dados fake/mock no frontend React, removendo mocks financeiros e mantendo só consumo real de API, com relatório final de arquivos alterados/removidos/estáticos e validação manual."
status: in_progress
created: 2026-04-09
---

# Quick Task 260409-lu3 Plan

## Objective

Eliminar exibição de dados financeiros inventados no frontend, preservando apenas dados vindos da API (ou estados vazios/loading/error quando não houver API para a tela).

## Tasks

### Task 1 — Dashboard: remover números e métricas estáticas

- Arquivo: `src/features/dashboard/pages/Dashboard.tsx`
- Ação:
  - Remover valores simulados fixos (ex.: `+ R$ 12.450`, score `82`, labels de otimização fixa, barra fixa `65%`, fallback `+0%`).
  - Exibir apenas métricas derivadas de `data` da API.
  - Quando não houver dados, mostrar estados vazios explícitos.
- Done:
  - Sem números financeiros hardcoded no Dashboard.
  - Todos os números exibidos vêm de `data` ou aparecem como `—`.

### Task 2 — Goals: remover lista fake de metas

- Arquivo: `src/features/goals/pages/Goals.tsx`
- Ação:
  - Remover array local com metas hardcoded (valores e progressos fictícios).
  - Substituir por estado vazio/informativo até existir endpoint real.
- Done:
  - Página não renderiza metas financeiras inventadas.
  - UX mantém feedback claro de indisponibilidade de dados reais.

### Task 3 — TransactionDetail: remover detalhe financeiro mockado

- Arquivo: `src/features/transactions/pages/TransactionDetail.tsx`
- Ação:
  - Trocar conteúdo fixo por carregamento real via API (`/api/transactions`) com busca por `id`.
  - Renderizar loading/not-found/error de forma explícita.
  - Remover textos de análise com percentuais simulados.
- Done:
  - Detalhe exibe apenas dados reais retornados pela API (ou estado vazio/erro).
  - Sem valores monetários hardcoded.

## Verification

1. `npm run build` deve concluir sem erros.
2. Navegar para Dashboard/Goals/TransactionDetail e confirmar:
   - nenhum número fake hardcoded visível;
   - estados vazios aparecem quando não há dados reais;
   - transação detalhada usa dados reais da API.

