---
phase: quick
plan: 260408-dk2
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schemas.py           # NOVO — criado
  - backend/core.py              # MODIFICADO — recebe format_brl
  - backend/modules/auth/router.py
  - backend/modules/categories/router.py
  - backend/modules/transactions/router.py
  - backend/modules/dashboard/services.py
  - backend/modules/auth/schemas.py           # DELETADO
  - backend/modules/categories/schemas.py     # DELETADO
  - backend/modules/transactions/schemas.py   # DELETADO
  - backend/modules/shared/utils.py           # DELETADO
autonomous: true
requirements: []

must_haves:
  truths:
    - "Todos os schemas Pydantic vivem em backend/schemas.py"
    - "format_brl vive em backend/core.py"
    - "Nenhum arquivo com < 5 linhas de lógica persiste no backend/"
    - "uvicorn backend.main:app sobe sem erros de ImportError"
  artifacts:
    - path: "backend/schemas.py"
      provides: "LoginRequest, SignUpRequest, CreateCategoryRequest, RenameCategoryRequest, CorrectTransactionRequest"
    - path: "backend/core.py"
      provides: "format_brl adicionado ao final do arquivo"
  key_links:
    - from: "backend/modules/auth/router.py"
      to: "backend/schemas.py"
      via: "from backend.schemas import LoginRequest, SignUpRequest"
    - from: "backend/modules/categories/router.py"
      to: "backend/schemas.py"
      via: "from backend.schemas import CreateCategoryRequest, RenameCategoryRequest"
    - from: "backend/modules/transactions/router.py"
      to: "backend/schemas.py"
      via: "from backend.schemas import CorrectTransactionRequest"
    - from: "backend/modules/dashboard/services.py"
      to: "backend/core.py"
      via: "from backend.core import format_brl"
---

<objective>
Consolidar e desfragmentar o backend Python eliminando arquivos tiny (< 10 linhas) sem alterar lógica de negócio.

Purpose: Reduzir a fragmentação em schemas dispersos (3 arquivos, 22 linhas no total) e a função utilitária isolada (3 linhas). Resultado: estrutura mais coesa, menos saltos de arquivo.

Output:
- backend/schemas.py (novo, ~25 linhas — todos os Pydantic models)
- backend/core.py (acrescenta format_brl no final)
- 4 arquivos deletados: 3x schemas.py dos módulos + shared/utils.py
- Pasta backend/modules/shared/ deletada (ficou vazia)
- Todos os imports atualizados
- Servidor sobe sem erros
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/LIMPEZA_BACKEND.md
@backend/main.py
@backend/core.py
@backend/modules/auth/router.py
@backend/modules/categories/router.py
@backend/modules/transactions/router.py
@backend/modules/dashboard/services.py

<interfaces>
<!-- Estado atual — o executor deve conhecer esses contratos antes de editar -->

backend/modules/auth/schemas.py (será DELETADO após migração):
```python
from pydantic import BaseModel, EmailStr, Field
class LoginRequest(BaseModel):
    email: EmailStr
    password: str = Field(min_length=6)
class SignUpRequest(BaseModel):
    email: EmailStr
    password: str = Field(min_length=6)
```

backend/modules/categories/schemas.py (será DELETADO após migração):
```python
from pydantic import BaseModel, Field
class CreateCategoryRequest(BaseModel):
    name: str = Field(min_length=1, max_length=50)
    color_hex: str = Field(min_length=4, max_length=7)
    emoji: str = Field(default="🏷️", max_length=4)
class RenameCategoryRequest(BaseModel):
    name: str = Field(min_length=1, max_length=50)
```

backend/modules/transactions/schemas.py (será DELETADO após migração):
```python
from pydantic import BaseModel
class CorrectTransactionRequest(BaseModel):
    category_id: str
```

backend/modules/shared/utils.py (será DELETADO após migração):
```python
def format_brl(value: float) -> str:
    """Formata um valor float para o padrao de moeda brasileiro (R$)."""
    return f"R$ {value:,.2f}".replace(",", "X").replace(".", ",").replace("X", ".")
```

Imports que precisam ser corrigidos:
- auth/router.py linha 5:      `from .schemas import LoginRequest, SignUpRequest`
- categories/router.py linha 6: `from .schemas import CreateCategoryRequest, RenameCategoryRequest`
- transactions/router.py linha 6: `from .schemas import CorrectTransactionRequest`
- dashboard/services.py linha 5: `from ..shared.utils import format_brl`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar backend/schemas.py com todos os modelos Pydantic</name>
  <files>backend/schemas.py</files>
  <action>
Criar o arquivo `backend/schemas.py` consolidando todos os modelos Pydantic dos três módulos. Não alterar nenhum model — copiar exatamente.

Conteúdo do arquivo:
```python
"""Schemas Pydantic centralizados do FinCoach API."""
from pydantic import BaseModel, EmailStr, Field


# --- Auth ---

class LoginRequest(BaseModel):
    email: EmailStr
    password: str = Field(min_length=6)


class SignUpRequest(BaseModel):
    email: EmailStr
    password: str = Field(min_length=6)


# --- Categories ---

class CreateCategoryRequest(BaseModel):
    name: str = Field(min_length=1, max_length=50)
    color_hex: str = Field(min_length=4, max_length=7)
    emoji: str = Field(default="🏷️", max_length=4)


class RenameCategoryRequest(BaseModel):
    name: str = Field(min_length=1, max_length=50)


# --- Transactions ---

class CorrectTransactionRequest(BaseModel):
    category_id: str
```

Verificar que o arquivo criado importa sem erro:
```
python -c "from backend.schemas import LoginRequest, SignUpRequest, CreateCategoryRequest, RenameCategoryRequest, CorrectTransactionRequest; print('OK')"
```
  </action>
  <verify>
    <automated>cd C:\Users\andre\Documents\projetofinanceiro && python -c "from backend.schemas import LoginRequest, SignUpRequest, CreateCategoryRequest, RenameCategoryRequest, CorrectTransactionRequest; print('schemas OK')"</automated>
  </verify>
  <done>backend/schemas.py existe e todos os 5 models importam sem erro</done>
</task>

<task type="auto">
  <name>Task 2: Mover format_brl para core.py e atualizar todos os imports</name>
  <files>
    backend/core.py,
    backend/modules/auth/router.py,
    backend/modules/categories/router.py,
    backend/modules/transactions/router.py,
    backend/modules/dashboard/services.py
  </files>
  <action>
**2a — Adicionar format_brl ao final de backend/core.py:**

Adicionar ao final de `backend/core.py` (após a última função existente):

```python

def format_brl(value: float) -> str:
    """Formata um valor float para o padrão de moeda brasileiro (R$)."""
    return f"R$ {value:,.2f}".replace(",", "X").replace(".", ",").replace("X", ".")
```

**2b — Atualizar import em backend/modules/auth/router.py:**

Substituir:
```python
from .schemas import LoginRequest, SignUpRequest
```
Por:
```python
from backend.schemas import LoginRequest, SignUpRequest
```

**2c — Atualizar import em backend/modules/categories/router.py:**

Substituir:
```python
from .schemas import CreateCategoryRequest, RenameCategoryRequest
```
Por:
```python
from backend.schemas import CreateCategoryRequest, RenameCategoryRequest
```

**2d — Atualizar import em backend/modules/transactions/router.py:**

Substituir:
```python
from .schemas import CorrectTransactionRequest
```
Por:
```python
from backend.schemas import CorrectTransactionRequest
```

**2e — Atualizar import em backend/modules/dashboard/services.py:**

Substituir:
```python
from ..shared.utils import format_brl
```
Por:
```python
from backend.core import format_brl
```

NÃO alterar nada mais — apenas os imports listados acima.
  </action>
  <verify>
    <automated>cd C:\Users\andre\Documents\projetofinanceiro && python -c "from backend.core import format_brl; print(format_brl(1234.56))"</automated>
  </verify>
  <done>format_brl em core.py; todos os imports dos 4 arquivos apontam para os novos destinos; format_brl('1234.56') retorna 'R$ 1.234,56'</done>
</task>

<task type="auto">
  <name>Task 3: Deletar arquivos obsoletos e verificar servidor</name>
  <files>
    backend/modules/auth/schemas.py,
    backend/modules/categories/schemas.py,
    backend/modules/transactions/schemas.py,
    backend/modules/shared/utils.py
  </files>
  <action>
**3a — Deletar os arquivos que ficaram obsoletos:**

```
backend/modules/auth/schemas.py
backend/modules/categories/schemas.py
backend/modules/transactions/schemas.py
backend/modules/shared/utils.py
```

**3b — Deletar a pasta backend/modules/shared/ se ficou vazia:**

Verificar se `backend/modules/shared/` contém apenas `utils.py`. Se sim, deletar a pasta inteira (incluindo qualquer `__init__.py` vazio).

**3c — Verificar backend/__init__.py:**

O arquivo contém apenas a docstring `"""Backend package for the FastAPI server."""` — deixar como está (não é um arquivo problemático).

**3d — Verificar o servidor sobe:**

Rodar uvicorn na porta 8099 brevemente para confirmar que não há ImportError:

```bash
uvicorn backend.main:app --port 8099 --timeout-graceful-shutdown 2
```

Aguardar a linha `Application startup complete.` nos logs. Se aparecer, interromper (Ctrl+C) e declarar sucesso. Se aparecer `ImportError` ou `ModuleNotFoundError`, corrigir antes de avançar.

**3e — Commit:**

```bash
git add -A
git commit -m "refactor(backend): consolidar schemas em backend/schemas.py e format_brl em core.py"
```
  </action>
  <verify>
    <automated>cd C:\Users\andre\Documents\projetofinanceiro && python -c "import backend.main; print('import OK')"</automated>
  </verify>
  <done>
  - Os 4 arquivos obsoletos não existem mais
  - backend/modules/shared/ deletada (ou inexistente)
  - `python -c "import backend.main"` não lança nenhum erro
  - Commit feito
  </done>
</task>

</tasks>

<verification>
Checklist final:
- [ ] `backend/schemas.py` existe com 5 models
- [ ] `backend/core.py` contém `format_brl` no final
- [ ] `backend/modules/auth/schemas.py` NÃO existe
- [ ] `backend/modules/categories/schemas.py` NÃO existe
- [ ] `backend/modules/transactions/schemas.py` NÃO existe
- [ ] `backend/modules/shared/utils.py` NÃO existe
- [ ] `python -c "import backend.main"` sem erros
- [ ] Nenhum arquivo com < 10 linhas de lógica real permanece no backend/
</verification>

<success_criteria>
- backend/schemas.py criado e importando corretamente
- format_brl disponível em backend.core
- 4 arquivos deletados sem deixar imports quebrados
- uvicorn sobe na porta 8099 sem ImportError
- Lógica de negócio 100% preservada (zero alterações em services.py e lógica de rotas)
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260408-dk2-limpeza-e-consolidacao-do-backend-python/260408-dk2-SUMMARY.md` com:
- O que foi consolidado
- Arquivos deletados
- Hash do commit
</output>
