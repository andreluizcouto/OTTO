---
phase: 03-ai-classification
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified:
  - .planning/phases/03-ai-classification/03-RESEARCH.md
  - tests/test_classifier.py
autonomous: false
requirements:
  - AICL-06
  - INTG-01
gap_closure: true
user_setup:
  - service: make_com
    why: "Runtime strict JSON schema enforcement is configured in external Make.com scenario"
    dashboard_config:
      - task: "Update Module 4 OpenAI HTTP body to include response_format.json_schema with strict=true and approved enum list"
        location: "Make.com Scenario -> Module 4 (HTTP OpenAI request)"
      - task: "Run one scenario execution and capture exported scenario JSON/screenshots proving strict schema is active"
        location: "Make.com Scenario History / Export"

must_haves:
  truths:
    - "With D-01 hybrid trigger behavior (auto on create/import + manual fallback/retry), classification still completes after custom categories and never fails on unknown/custom slugs because mapping is deterministic (custom→canonical or fallback 'outros' with low-confidence review)."
    - "Runtime integration has executable/config-as-code evidence that OpenAI call enforces strict JSON schema."
    - "Schema contract (name, strict=true, required fields, enum slugs) is versioned and test-validated in repository."
    - "Structured output contract evidence is auditable for re-verification."
  artifacts:
    - path: ".planning/phases/03-ai-classification/03-RESEARCH.md"
      provides: "Versioned Make.com runtime configuration contract with strict schema snippet and verification checklist"
      contains: "response_format"
    - path: "tests/test_classifier.py"
      provides: "Automated strict-schema contract checks aligned to Make.com runtime body"
      contains: "test_json_schema_structure"
  key_links:
    - from: "src/data/classifier.py#get_openai_json_schema"
      to: "Make.com Module 4 HTTP request body"
      via: "same strict schema fields and enum list documented in config-as-code section"
      pattern: "strict"
    - from: ".planning/phases/03-ai-classification/03-RESEARCH.md"
      to: "03-VERIFICATION re-run"
      via: "human-checkable runtime evidence checklist"
      pattern: "evidence"
---

<objective>
Close the structured-outputs gap with auditable runtime config-as-code evidence for Make.com integration.

Purpose: Verification flagged AICL-06 as partial because strict JSON schema existed only as Python helper/documentation but lacked runtime-enforced evidence for Make.com.
Output: Versioned runtime contract + automated schema assertions + explicit human verification checklist tied to Make.com execution artifacts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-classification/03-CONTEXT.md
@.planning/phases/03-ai-classification/03-VERIFICATION.md
@.planning/phases/03-ai-classification/03-RESEARCH.md
@src/data/classifier.py
@tests/test_classifier.py

<interfaces>
From src/data/classifier.py:
```python
def get_openai_json_schema() -> dict[str, Any]:
    # returns {"name":"classification_result","strict":True,"schema":{...}}
```

Expected Make.com Module 4 fragment:
```json
"response_format": {
  "type": "json_schema",
  "json_schema": { "...strict schema..." }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden executable schema contract tests for strict runtime parity</name>
  <files>tests/test_classifier.py</files>
  <read_first>
    - tests/test_classifier.py
    - src/data/classifier.py
    - .planning/phases/03-ai-classification/03-VERIFICATION.md
  </read_first>
  <behavior>
    - Test asserts strict=True, required fields, additionalProperties=False, and canonical slug enum.
    - Test validates schema object shape matches the Make.com Module 4 `json_schema` payload contract.
  </behavior>
  <action>
    Extend/clarify schema tests so they act as executable contract for runtime configuration:
    - keep existing json schema checks,
    - add explicit assertion comments/names showing this is the exact object to paste under Make.com `response_format.json_schema`,
    - verify enum and required fields are exhaustive and deterministic,
    - codify deterministic slug contract: OpenAI output remains strict canonical enum; Make.com must map custom category slugs to canonical enum before write-back, and fallback to `outros` + low-confidence when no mapping exists.
    This addresses verification concern by making schema drift detectable in CI before Make.com mismatch reaches runtime.
  </action>
  <acceptance_criteria>
    - tests/test_classifier.py includes strict-runtime-parity assertions for get_openai_json_schema().
    - Schema test failure clearly indicates what Make.com field would be invalid.
    - Tests/documentation explicitly define deterministic behavior for custom category slugs (map to canonical enum or fallback `outros`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_classifier.py::test_json_schema_structure -q</automated>
  </verify>
  <done>Strict schema contract is executable and enforced by automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture runtime Make.com strict-schema evidence and pin it in repo docs</name>
  <read_first>
    - .planning/phases/03-ai-classification/03-RESEARCH.md
    - src/data/classifier.py (get_openai_json_schema)
  </read_first>
  <what-built>
    Automated contract tests now lock the expected strict schema. This checkpoint verifies the external Make.com runtime uses the same strict schema in production flow and records evidence in versioned project docs.
  </what-built>
  <how-to-verify>
    1. Open Make.com Scenario used by Phase 3 classification.
     2. In Module 4 (OpenAI HTTP request), confirm request body contains:
        - response_format.type = "json_schema"
        - response_format.json_schema.name = "classification_result"
        - response_format.json_schema.strict = true
        - required: ["category_slug","confidence"]
        - additionalProperties = false
     3. Confirm deterministic slug handling contract is implemented in scenario:
        - OpenAI output is restricted to canonical enum slugs only.
        - If downstream logic receives custom/unknown slug, scenario applies deterministic mapping to canonical slug; when no mapping exists, uses `outros` and marks low confidence for inline review.
     4. Run one classification execution in Make.com and inspect scenario execution details for Module 4 request payload.
     5. Update `.planning/phases/03-ai-classification/03-RESEARCH.md` with a short “Runtime Evidence (AICL-06)” subsection including:
        - date/time checked,
        - scenario identifier,
        - confirmation that strict schema fields above are present,
        - explicit note of custom/unknown slug deterministic handling,
        - any mismatch notes.
     6. Re-run `python -m pytest tests/test_classifier.py::test_json_schema_structure -q`.
  </how-to-verify>
  <resume-signal>Type "runtime schema evidence captured" after Make.com runtime checks are completed and RESEARCH.md updated.</resume-signal>
  <action>
    Record only factual evidence (no speculative text). If runtime differs from tested schema, explicitly note mismatch and stop for follow-up fix before verification rerun.
  </action>
  <acceptance_criteria>
    - RESEARCH.md contains Runtime Evidence subsection with concrete runtime confirmation points.
    - Human confirms Module 4 uses strict json_schema at runtime.
    - RESEARCH.md captures deterministic custom/unknown slug handling contract and fallback behavior.
    - Schema contract test still passes after documentation update.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_classifier.py::test_json_schema_structure -q</automated>
  </verify>
  <done>Structured outputs are backed by both executable tests and runtime evidence for Make.com integration.</done>
</task>

</tasks>

<verification>
Gap closure verification targets:
- AICL-06 transitions from partial to verifiable with runtime evidence.
- INTG-01 evidence path includes external scenario request-body proof.
- Contract test remains green.
</verification>

<success_criteria>
- Strict JSON schema enforcement is demonstrably configured in Make.com runtime integration.
- Repository contains versioned, auditable proof path for future verification runs.
- No regression in classifier schema contract tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-classification/03-05-SUMMARY.md`
</output>
