---
phase: 03-ai-classification
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - src/data/classifier.py
  - src/pages/settings.py
  - tests/test_classifier.py
  - tests/test_settings.py
autonomous: true
requirements:
  - AICL-01
  - AICL-02
  - AICL-03
  - AICL-04
  - INTG-01
gap_closure: true

must_haves:
  truths:
    - "D-01 hybrid behavior is preserved: newly created/imported transactions are classified automatically in the live creation flow, while the manual classify CTA on Transactions remains available as fallback/retry."
    - "Runtime webhook payload uses normalized merchant names (e.g., RCHLO → Riachuelo) before Make.com/OpenAI classification."
    - "Confidence and low-confidence review behavior remains intact after the new automatic trigger wiring."
  artifacts:
    - path: "src/pages/settings.py"
      provides: "Automatic post-create classification trigger for newly inserted transactions"
      contains: "trigger_classification("
    - path: "src/data/classifier.py"
      provides: "Runtime merchant normalization in payload builder"
      contains: "resolve_merchant_name("
    - path: "tests/test_settings.py"
      provides: "Executable regression for automatic trigger on data creation flow"
      contains: "test_generate_data_triggers_automatic_classification"
  key_links:
    - from: "src/pages/settings.py"
      to: "src/data/classifier.py"
      via: "automatic call to trigger_classification() after transaction creation"
      pattern: "trigger_classification\\("
    - from: "src/data/classifier.py"
      to: "Make.com webhook payload"
      via: "build_classification_payload normalizes merchant_name values"
      pattern: "resolve_merchant_name\\("
---

<objective>
Close verification gaps for automatic trigger and merchant normalization in live classification flow.

Purpose: Ensure AICL-01/AICL-02 are true in runtime execution (not only helpers), while preserving locked decisions (D-01 hybrid auto+manual fallback behavior, D-02 idempotent scope).
Output: Runtime wiring + targeted tests proving auto-trigger and normalization happen in the executed path.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-classification/03-CONTEXT.md
@.planning/phases/03-ai-classification/03-VERIFICATION.md
@.planning/phases/03-ai-classification/03-RESEARCH.md
@src/pages/settings.py
@src/data/classifier.py
@tests/test_classifier.py

<interfaces>
From src/data/classifier.py:
```python
def build_classification_payload(user_id: str, transactions: list[dict], categories: list[dict]) -> dict: ...
def resolve_merchant_name(merchant: str) -> str: ...
def trigger_classification(client: Any, user_id: str) -> dict[str, Any]: ...
```

From src/pages/settings.py:
```python
transactions = generate_transactions(user["id"], category_map)
client.table("transactions").insert(transactions).execute()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing/then-passing tests for automatic trigger and runtime normalization</name>
  <files>tests/test_classifier.py, tests/test_settings.py</files>
  <read_first>
    - .planning/phases/03-ai-classification/03-VERIFICATION.md
    - tests/test_classifier.py
    - src/pages/settings.py
  </read_first>
  <behavior>
    - Test: build_classification_payload normalizes merchant_name in outgoing transactions list (AICL-02 gap).
    - Test: settings data creation flow triggers classification automatically after insert success (AICL-01 gap).
    - Test: no regression in confidence behavior assumptions used by transactions page (AICL-03/AICL-04 guard).
  </behavior>
  <action>
    Create/extend tests to codify the two failed truths from verification:
    1) payload merchant normalization is applied in runtime path (per D-07 integration flow),
    2) automatic classification is invoked in the new-transaction creation path.

    Keep D-01 hybrid behavior intact by asserting manual CTA fallback is not removed while creation/import flow also triggers classification automatically.
  </action>
  <acceptance_criteria>
    - New tests fail before implementation and pass after implementation.
    - A test named test_generate_data_triggers_automatic_classification exists in tests/test_settings.py.
    - A classifier payload test verifies normalized merchant_name for RCHLO/PAG*/NF* examples.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_classifier.py tests/test_settings.py -q</automated>
  </verify>
  <done>Executable tests now cover both gap behaviors and protect them against regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime auto-trigger and merchant normalization wiring</name>
  <files>src/data/classifier.py, src/pages/settings.py</files>
  <read_first>
    - src/data/classifier.py
    - src/pages/settings.py
    - .planning/phases/03-ai-classification/03-CONTEXT.md
  </read_first>
  <behavior>
    - New transactions created/imported via live generation flow trigger classification automatically.
    - build_classification_payload sends normalized merchant_name values to webhook payload.
    - D-01 hybrid trigger remains intact (auto in creation/import flow + manual fallback CTA) and D-02 idempotent scope is preserved.
  </behavior>
  <action>
    Update runtime flow with minimal surface area:
    - In src/data/classifier.py, wire resolve_merchant_name() into build_classification_payload so outgoing merchant_name is normalized before webhook send (addresses verification gap reason directly).
    - In src/pages/settings.py, after successful transaction creation insert, invoke trigger_classification(client, user["id"]) automatically and present success/error UX copy consistent with 03-UI-SPEC.
    - Preserve existing manual trigger in Transactions page (per D-01) and retain idempotent filtering semantics (per D-02).
  </action>
  <acceptance_criteria>
    - Creating new transactions runs trigger_classification without manual button interaction.
    - Payload sent from classifier contains normalized merchant names for known cryptic codes.
    - No regressions in category CRUD and transactions review behavior.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_classifier.py tests/test_settings.py tests/test_transactions.py tests/test_categories.py -q</automated>
  </verify>
  <done>Automatic classification + runtime normalization are wired in live flow and verified by tests.</done>
</task>

</tasks>

<verification>
Run targeted gap closure checks:
- Automatic trigger path test passes.
- Merchant normalization runtime payload test passes.
- Existing Phase 3 behavior tests remain green.
</verification>

<success_criteria>
- Verification truth #1 is now satisfiable: new transactions classify automatically in at least one live creation/import path.
- Verification truth #2 is now satisfiable: merchant normalization is connected to runtime payload path.
- AICL-01, AICL-02, AICL-03, AICL-04, and INTG-01 remain covered with green automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-classification/03-04-SUMMARY.md`
</output>
