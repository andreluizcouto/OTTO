---
phase: 02-data-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/ui/charts.py
  - src/data/__init__.py
  - tests/__init__.py
  - tests/test_generator.py
  - tests/test_charts.py
  - tests/test_dashboard.py
  - pyproject.toml
autonomous: true
requirements:
  - DATA-02
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "get_authenticated_client() returns a Supabase client with user JWT set via set_session"
    - "Chart builder functions return valid Plotly Figure objects"
    - "Test suite runs and validates generator output, chart creation, and date filter logic"
  artifacts:
    - path: "src/config.py"
      provides: "get_authenticated_client() function"
      contains: "def get_authenticated_client"
    - path: "src/ui/charts.py"
      provides: "Plotly chart builders for donut, trend, comparison"
      exports: ["create_donut_chart", "create_trend_chart", "create_comparison_chart", "PLOTLY_LAYOUT", "format_brl"]
    - path: "tests/test_generator.py"
      provides: "Unit tests for transaction generator"
      contains: "def test_"
    - path: "tests/test_charts.py"
      provides: "Unit tests for chart creation"
      contains: "def test_donut_chart"
    - path: "tests/test_dashboard.py"
      provides: "Unit tests for date filter logic"
      contains: "def test_date_filters"
    - path: "pyproject.toml"
      provides: "pytest configuration"
      contains: "tool.pytest"
  key_links:
    - from: "src/ui/charts.py"
      to: "plotly.graph_objects"
      via: "import go"
      pattern: "import plotly.graph_objects as go"
    - from: "src/config.py"
      to: "supabase client"
      via: "set_session"
      pattern: "client.auth.set_session"
---

<objective>
Create the authenticated Supabase client helper, Plotly chart builder module, and Wave 0 test infrastructure for Phase 2.

Purpose: All subsequent plans depend on the authenticated client (for RLS-compliant DB ops) and chart builders (for dashboard rendering). Tests must exist before implementation to validate generator output, chart creation, and date filter correctness.

Output: `src/config.py` with `get_authenticated_client()`, `src/ui/charts.py` with three chart builders + shared layout config + currency formatter, test suite in `tests/`, pytest config in `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-dashboard/02-CONTEXT.md
@.planning/phases/02-data-dashboard/02-RESEARCH.md
@.planning/phases/02-data-dashboard/02-UI-SPEC.md
@.planning/phases/02-data-dashboard/02-VALIDATION.md
@src/config.py
@src/auth.py
@src/ui/styles.py
@supabase/schema.sql
@supabase/seed_categories.sql

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/config.py:
```python
def get_supabase_url() -> str: ...
def get_supabase_anon_key() -> str: ...
def get_supabase_client() -> Client: ...
```

From src/auth.py:
```python
def get_current_user() -> dict | None: ...  # returns {"id": str, "email": str, "created_at": str}
def is_authenticated() -> bool: ...
```

Session state keys (set by sign_in):
- st.session_state["access_token"] -> str (JWT)
- st.session_state["refresh_token"] -> str
- st.session_state["user"] -> dict with "id", "email", "created_at"

From supabase/seed_categories.sql (category slugs):
alimentacao, transporte, moradia, saude, lazer, educacao, compras, assinaturas, delivery, outros
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticated client + chart builders + currency formatter</name>
  <files>src/config.py, src/ui/charts.py, src/data/__init__.py</files>
  <read_first>src/config.py, src/ui/styles.py, supabase/seed_categories.sql, .planning/phases/02-data-dashboard/02-UI-SPEC.md, .planning/phases/02-data-dashboard/02-RESEARCH.md</read_first>
  <behavior>
    - get_authenticated_client() creates a Supabase client and calls set_session with access_token and refresh_token from st.session_state
    - get_authenticated_client() returns a plain client if session_state has no tokens (graceful fallback)
    - format_brl(1234.56) returns "R$ 1.234,56"
    - format_brl(0) returns "R$ 0,00"
    - format_brl(1000000.99) returns "R$ 1.000.000,99"
    - create_donut_chart returns a plotly Figure with Pie trace, hole=0.55
    - create_trend_chart returns a plotly Figure with Scatter trace, mode='lines+markers', fill='tozeroy'
    - create_comparison_chart returns a plotly Figure with 2 Bar traces, barmode='group'
    - All charts use transparent background (paper_bgcolor, plot_bgcolor)
    - All charts use Inter font family
  </behavior>
  <action>
1. **Add `get_authenticated_client()` to `src/config.py`:**
   - Add import: `import streamlit as st` (already imported by other modules but config.py needs it)
   - Add function after existing `get_supabase_client()`:
   ```python
   def get_authenticated_client() -> Client:
       """Create Supabase client with user's JWT session for RLS-compliant operations."""
       url = get_supabase_url()
       key = get_supabase_anon_key()
       client = create_client(url, key)
       access_token = st.session_state.get("access_token")
       refresh_token = st.session_state.get("refresh_token")
       if access_token and refresh_token:
           client.auth.set_session(access_token, refresh_token)
       return client
   ```
   - Keep existing functions unchanged.

2. **Create `src/data/__init__.py`:** Empty file to make `src/data/` a Python package.

3. **Create `src/ui/charts.py`** with the following exact exports:

   - `PLOTLY_LAYOUT` dict constant:
     ```python
     PLOTLY_LAYOUT = dict(
         paper_bgcolor="rgba(0,0,0,0)",
         plot_bgcolor="rgba(0,0,0,0)",
         font=dict(family="Inter", color="#94A3B8", size=14),
         margin=dict(l=0, r=0, t=32, b=0),
         legend=dict(font=dict(size=14, color="#94A3B8")),
         xaxis=dict(gridcolor="rgba(51,65,85,0.3)", zerolinecolor="#334155"),
         yaxis=dict(gridcolor="rgba(51,65,85,0.3)", zerolinecolor="#334155"),
     )
     ```

   - `format_brl(value: float) -> str` function:
     ```python
     def format_brl(value: float) -> str:
         return f"R$ {value:,.2f}".replace(",", "X").replace(".", ",").replace("X", ".")
     ```

   - `create_donut_chart(df_categories: pd.DataFrame, total_formatted: str) -> go.Figure`:
     - `df_categories` must have columns: `category_name`, `amount`, `color_hex`
     - Creates `go.Pie` with `hole=0.55`, `marker=dict(colors=df_categories["color_hex"].tolist())`, `textinfo="percent"`, `hovertemplate="%{label}<br>R$ %{value:,.2f}<br>%{percent}<extra></extra>"`
     - Center annotation: `total_formatted` at x=0.5, y=0.5, font size 20, color `#F8FAFC`, family `Inter`, `showarrow=False`
     - Apply `**PLOTLY_LAYOUT` via `update_layout`

   - `create_trend_chart(df_trend: pd.DataFrame) -> go.Figure`:
     - `df_trend` must have columns: `period_label`, `total_amount`
     - Creates `go.Scatter` with `mode="lines+markers"`, `line=dict(color="#2563EB", width=2)`, `marker=dict(size=6, color="#2563EB")`, `fill="tozeroy"`, `fillcolor="rgba(37, 99, 235, 0.1)"`, `hovertemplate="R$ %{y:,.2f}<extra></extra>"`
     - Apply `**PLOTLY_LAYOUT` via `update_layout`

   - `create_comparison_chart(categories: list, current_amounts: list, previous_amounts: list) -> go.Figure`:
     - Creates two `go.Bar` traces: name="Mes atual" with `marker_color="#2563EB"`, name="Mes anterior" with `marker_color="#475569"`
     - Apply `**PLOTLY_LAYOUT` via `update_layout` plus `barmode="group"`, `bargap=0.2`, `bargroupgap=0.1`

   - Imports at top: `import plotly.graph_objects as go`, `import pandas as pd`
  </action>
  <verify>
    <automated>python -c "from src.ui.charts import create_donut_chart, create_trend_chart, create_comparison_chart, PLOTLY_LAYOUT, format_brl; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/config.py contains `def get_authenticated_client()` with `client.auth.set_session(access_token, refresh_token)`
    - src/config.py still contains `def get_supabase_client()` (unchanged)
    - src/ui/charts.py contains `PLOTLY_LAYOUT = dict(`
    - src/ui/charts.py contains `def format_brl(value: float) -> str:`
    - src/ui/charts.py contains `def create_donut_chart(`
    - src/ui/charts.py contains `def create_trend_chart(`
    - src/ui/charts.py contains `def create_comparison_chart(`
    - src/ui/charts.py contains `hole=0.55`
    - src/ui/charts.py contains `fill="tozeroy"`
    - src/ui/charts.py contains `barmode="group"`
    - src/ui/charts.py contains `paper_bgcolor="rgba(0,0,0,0)"`
    - src/data/__init__.py exists
  </acceptance_criteria>
  <done>Authenticated client helper added to config.py. Chart builder module created with donut, trend, comparison functions plus shared layout and currency formatter. Data package initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test infrastructure</name>
  <files>tests/__init__.py, tests/test_generator.py, tests/test_charts.py, tests/test_dashboard.py, pyproject.toml</files>
  <read_first>src/ui/charts.py, .planning/phases/02-data-dashboard/02-VALIDATION.md, .planning/phases/02-data-dashboard/02-UI-SPEC.md, supabase/schema.sql</read_first>
  <behavior>
    - test_generator.py: validate generated transaction dicts have required keys (user_id, amount, date, description, merchant_name, category_id, confidence_score, payment_method, is_recurring)
    - test_generator.py: validate amounts are within declared ranges per category
    - test_generator.py: validate dates are within 3-month window
    - test_generator.py: validate category distribution is weighted (alimentacao appears more than outros)
    - test_charts.py: test_donut_chart creates Figure with Pie trace
    - test_charts.py: test_trend_chart creates Figure with Scatter trace
    - test_charts.py: test_comparison_chart creates Figure with 2 Bar traces
    - test_charts.py: test_format_brl validates Brazilian currency formatting
    - test_dashboard.py: test_date_filters validates "Esta semana" returns Monday of current week
    - test_dashboard.py: test_date_filters validates "Este mes" returns 1st of current month
    - test_dashboard.py: test_date_filters validates "Ultimos 3 meses" returns 1st of month ~3 months ago
  </behavior>
  <action>
1. **Create `pyproject.toml`** with pytest config:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   python_files = ["test_*.py"]
   python_functions = ["test_*"]
   addopts = "-x -q"
   ```

2. **Create `tests/__init__.py`:** Empty file.

3. **Create `tests/test_generator.py`:**
   - Import: `from src.data.generator import generate_transactions, MERCHANTS, AMOUNT_RANGES, CATEGORY_WEIGHTS`
   - Note: `generate_transactions` does NOT exist yet -- tests will fail (RED phase). That is intentional. The generator will be created in Plan 02.
   - Tests:
     - `test_generate_returns_list`: `result = generate_transactions("fake-uid", mock_category_map)` returns a list with length between 120 and 180 (3 months * 40-60 per month).
     - `test_transaction_schema`: Each dict in result has keys: `user_id`, `amount`, `date`, `description`, `merchant_name`, `category_id`, `confidence_score`, `payment_method`, `is_recurring`.
     - `test_amount_ranges`: For each transaction, find category slug from mock_category_map, verify `AMOUNT_RANGES[slug][0] <= txn["amount"] <= AMOUNT_RANGES[slug][1]`.
     - `test_dates_within_range`: All transaction dates are within 3 months of today.
     - `test_category_distribution`: Over 150+ transactions, "alimentacao" count > "outros" count (weighted distribution).
     - Create a `mock_category_map` fixture that maps slugs to dicts like `{"id": "fake-uuid-alimentacao", ...}` for all 10 categories.

4. **Create `tests/test_charts.py`:**
   - Import: `from src.ui.charts import create_donut_chart, create_trend_chart, create_comparison_chart, format_brl`
   - Import: `import pandas as pd`, `import plotly.graph_objects as go`
   - Tests:
     - `test_format_brl_basic`: `assert format_brl(1234.56) == "R$ 1.234,56"`
     - `test_format_brl_zero`: `assert format_brl(0) == "R$ 0,00"`
     - `test_format_brl_large`: `assert format_brl(1000000.99) == "R$ 1.000.000,99"`
     - `test_donut_chart`: Create sample DataFrame with columns `category_name`, `amount`, `color_hex` (3 rows). Call `create_donut_chart(df, "R$ 1.000,00")`. Assert `isinstance(fig, go.Figure)`. Assert `fig.data[0].hole == 0.55`. Assert `len(fig.data) == 1`. Assert `fig.layout.annotations[0].text == "R$ 1.000,00"`.
     - `test_trend_chart`: Create sample DataFrame with `period_label`, `total_amount` (4 rows). Call `create_trend_chart(df)`. Assert `isinstance(fig, go.Figure)`. Assert `fig.data[0].mode == "lines+markers"`. Assert `fig.data[0].fill == "tozeroy"`.
     - `test_comparison_chart`: Call `create_comparison_chart(["A", "B"], [100, 200], [80, 150])`. Assert `isinstance(fig, go.Figure)`. Assert `len(fig.data) == 2`. Assert `fig.layout.barmode == "group"`.

5. **Create `tests/test_dashboard.py`:**
   - Import: `from datetime import date, timedelta`
   - Define a pure function `calculate_date_range(period: str, today: date) -> tuple[date, date]` test target. This function will be extracted into a utility in Plan 03. For now, write the tests against the expected behavior:
     - `test_date_filter_this_week`: Given `today = date(2026, 4, 6)` (Monday), `calculate_date_range("Esta semana", today)` returns `(date(2026, 4, 6), date(2026, 4, 6))`. Given `today = date(2026, 4, 8)` (Wednesday), start is `date(2026, 4, 6)` (Monday).
     - `test_date_filter_this_month`: Given `today = date(2026, 4, 15)`, returns `(date(2026, 4, 1), date(2026, 4, 15))`.
     - `test_date_filter_last_3_months`: Given `today = date(2026, 4, 15)`, returns `(date(2026, 2, 1), date(2026, 4, 15))` -- start is 1st of month 2 months prior.
   - Note: The `calculate_date_range` function does NOT exist yet. Import it as `from src.pages.dashboard import calculate_date_range`. Tests will FAIL until Plan 03 implements it.
   - Mark generator and dashboard tests that import not-yet-created modules with `pytest.importorskip` or wrap with try/except so the chart tests (which CAN pass now) are not blocked. Use `pytest.mark.skip(reason="Implemented in Plan 02")` for generator tests and `pytest.mark.skip(reason="Implemented in Plan 03")` for dashboard date filter tests so the test suite does not error out before those plans execute.
  </action>
  <verify>
    <automated>cd /c/Users/andre/Documents/projetofinanceiro && python -m pytest tests/test_charts.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `[tool.pytest.ini_options]` with `testpaths = ["tests"]`
    - tests/__init__.py exists
    - tests/test_generator.py contains `def test_generate_returns_list`
    - tests/test_generator.py contains `def test_transaction_schema`
    - tests/test_generator.py contains `def test_amount_ranges`
    - tests/test_generator.py contains `def test_dates_within_range`
    - tests/test_generator.py contains `def test_category_distribution`
    - tests/test_charts.py contains `def test_format_brl_basic`
    - tests/test_charts.py contains `def test_donut_chart`
    - tests/test_charts.py contains `def test_trend_chart`
    - tests/test_charts.py contains `def test_comparison_chart`
    - tests/test_dashboard.py contains `def test_date_filter_this_week`
    - tests/test_dashboard.py contains `def test_date_filter_this_month`
    - tests/test_dashboard.py contains `def test_date_filter_last_3_months`
    - `python -m pytest tests/test_charts.py -x -v` exits 0 (chart tests pass against charts.py)
    - Generator tests are marked skip (will pass after Plan 02)
    - Dashboard date filter tests are marked skip (will pass after Plan 03)
  </acceptance_criteria>
  <done>Test infrastructure created with pytest config, chart tests passing, generator and dashboard tests scaffolded as skipped pending implementation in Plans 02 and 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->Supabase | Unauthenticated client cannot read/write RLS-protected tables |
| session_state->client | JWT tokens from session_state used to authenticate Supabase client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | get_authenticated_client | mitigate | Function checks for both access_token AND refresh_token before calling set_session; returns unauthenticated client if missing (no data leaks since RLS blocks) |
| T-02-02 | Elevation of Privilege | chart builders | accept | Charts are pure rendering functions with no DB access; no privilege boundary crossed |
</threat_model>

<verification>
- `python -c "from src.config import get_authenticated_client; print('OK')"` succeeds
- `python -c "from src.ui.charts import create_donut_chart, create_trend_chart, create_comparison_chart, format_brl, PLOTLY_LAYOUT; print('OK')"` succeeds
- `python -m pytest tests/test_charts.py -x -v` all pass
- `python -m pytest tests/ -x -v` runs without errors (skipped tests are OK)
</verification>

<success_criteria>
- get_authenticated_client() exists in config.py with set_session call
- Chart builder module exports all three chart functions + layout config + currency formatter
- All chart tests pass
- Generator and dashboard tests exist as skipped scaffolds
- pytest configured in pyproject.toml
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-dashboard/02-01-SUMMARY.md`
</output>
